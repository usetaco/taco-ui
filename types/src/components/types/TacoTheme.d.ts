export declare class TacoTheme {
    primaryColor: string;
    secondaryColor: string;
    fontFamily: string;
    chakraTheme: Record<string, any>;
    constructor(primaryColor: string, secondaryColor: string, fontFamily: string, componentOverrides?: any);
}
